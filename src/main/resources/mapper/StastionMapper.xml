<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="StastionMapper">

<!--    <select id="getProjectList" resultType="com.jen.exceldevice.pojo.Project">-->
<!--		select * from tbl_project-->
<!--	</select>-->

	<select id="getStastionListById" resultType="com.jen.exceldevice.pojo.Stastion" parameterType="java.lang.Integer">
		select * from tbl_stastion
		<where>
			<if test="project_id!= 0">
				AND project_id = #{project_id,jdbcType=INTEGER}
			</if>
		</where>
	</select>

<!--<insert id="insertProject" parameterType="com.jen.exceldevice.pojo.Project">-->
<!--	insert into project (id,name)-->
<!--	values(#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR})-->
<!--</insert>-->





</mapper>