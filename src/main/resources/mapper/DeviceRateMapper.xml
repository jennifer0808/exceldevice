<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jen.exceldevice.mapper.DeviceRateMapper">
    <select id="getRateListBy" parameterType="com.jen.exceldevice.pojo.DeviceRate">
        select * from tbl_device_rate where  combine_name = #{combine_name,jdbcType=VARCHAR} and rate = #{rate,jdbcType=DECIMAL}
    </select>

    <insert id=""


</mapper>